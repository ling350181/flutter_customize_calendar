name: flutter_customize_calendar
description: A new flutter plugin project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.16.1

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:
  plugin:
    platforms:
      android:
        package: com.xunjin.flutter_customize_calendar
        pluginClass: FlutterCustomizeCalendarPlugin
      ios:
        pluginClass: FlutterCustomizeCalendarPlugin
